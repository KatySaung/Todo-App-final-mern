import Delete from "../../components/Delete/Delete"

// @Pages,Delete
// route : 
function Delete ({user, setUser}) {
    return (
        <Delete user={user} setUser={setUser} />
    )
  }
  
  export default Delete
